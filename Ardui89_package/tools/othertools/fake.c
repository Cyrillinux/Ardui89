#include <stdio.h>
int main(int argc, char *argv[]) {
    /* Debug
    // See all args
    for (int i = 0; i < argc; i++) {
        printf("argv[%d] = %s\n", i, argv[i]);
    };
    return -1;
     */
    return 0;
};

